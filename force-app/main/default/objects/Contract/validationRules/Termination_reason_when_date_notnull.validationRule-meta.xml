<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Termination_reason_when_date_notnull</fullName>
    <active>true</active>
    <description>when the termination date is not null you must populate the reason</description>
    <errorConditionFormula>AND(
    NOT(ISNULL(Contract_Termination_Date__c)), 
    ISBLANK(TEXT( Contract_Termination_Reason__c ))
    )</errorConditionFormula>
    <errorMessage>Please populate the contract termination reason</errorMessage>
</ValidationRule>
