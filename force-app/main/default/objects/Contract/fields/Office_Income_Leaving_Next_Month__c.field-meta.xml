<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Office_Income_Leaving_Next_Month__c</fullName>
    <formula>IF(
  AND(
    MONTH(Calculated_End_Date__c) = MONTH(TODAY()),
    YEAR(Calculated_End_Date__c) = YEAR(TODAY())
  ),
  Office_Income__c,
  0
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Office Income Leaving Next Month</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <type>Currency</type>
</CustomField>
